{
  "name": "loaf-gas-dashboard",
  "version": "1.0.0",
  "private": true,
  "description": "Settlement gas dashboard for Arbitrum Sepolia \u2014 collector + static GitHub Pages site (no dependencies)",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "collect": "node collect.mjs",
    "serve": "node serve.mjs",
    "test": "node --test"
  }
}
